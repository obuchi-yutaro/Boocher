.book-content  
  .single-box{ :style => "background-image: url(#{book.image})" } 
  .status
    %ul
      %li Title
      %a #{book.title} 
      %li  Genre
      %a #{book.genre}
      %li  Author
      %a #{book.author}
      %li
        Reviewer
        = link_to "#{book.user.nickname}", user_path(book.id), method: :get, class:"mypage"

.link-show
  = link_to "詳細", book_path(book.id), method: :get